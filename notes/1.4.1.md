# ModbusLink v1.4.1 版本发布说明

## 新功能：支持 RS485 模式 (1.4.0 基础上)
在 `rtu_transport.py` 和 `ascii_transport.py` 中添加了原生 RS485 模式支持，通过 RTS 信号实现软件控制的 RS485 总线方向。

```python
from serial.rs485 import RS485Settings
from modbuslink import SyncRtuTransport

rs485_settings = RS485Settings(
    rts_level_for_tx=True,
    rts_level_for_rx=False,
    delay_before_tx=0.001,
    delay_before_rx=0.001,
)
transport = SyncRtuTransport('/dev/ttyUSB0', rs485_mode=rs485_settings)
```

## API 新增内容

1. [rtu_transport.py](../src/modbuslink/transport/rtu_transport.py)：
    - `SyncRtuTransport`新增了 `rs485_mode: Optional[RS485Settings] = None` 参数
    - `AsyncRtuTransport`新增了 `rs485_mode: Optional[RS485Settings] = None` 参数

2. [ascii_transport.py](../src/modbuslink/transport/ascii_transport.py)：
    - `SyncAsciiTransport`新增了 `rs485_mode: Optional[RS485Settings] = None` 参数
    - `AsyncAsciiTransport`新增了 `rs485_mode: Optional[RS485Settings] = None` 参数

3. [README.md](../README.md) & [README-zh_CN.md](../README-zh_CN.md)：
   - 添加了 RS485 模式说明

4. [client/__init__.py](../src/modbuslink/client/__init__.py) & [common/__init__.py](../src/modbuslink/common/__init__.py) & [server/__init__.py](../src/modbuslink/server/__init__.py) & [transport/__init__.py](../src/modbuslink/transport/__init__.py) & [utils/__init__.py](../src/modbuslink/utils/__init__.py)：
   - 完成 `__all__` 导出列表
